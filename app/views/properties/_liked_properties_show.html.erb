<% if user_signed_in? && @property.likes.where(user_id: current_user).any?# # la property a ét élike %>
This property is in your favorites
<% else %>
<%= link_to property_likes_path(@property), class: "btn btn-outline-secondary", method: :post, remote: true do %>
Save to Favorites<i class="fas fa-heart" style="color:#f62928; font-size: 20px; padding: 5px;"></i>
<% end %>
<% end %>
